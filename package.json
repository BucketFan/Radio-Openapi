{
  "scripts": {
    "build:doc": "redoc-cli build openapi.yaml -o document/index.html",
    "build:packages": "sh ./scripts/generate_packages.sh",
    "mockserver": "prism mock ./openapi.yaml",
    "lint": "npx @stoplight/spectral-cli lint openapi.yaml"
  },
  "dependencies": {
    "openapi-typescript": "^5.4.1",
    "redoc-cli": "^0.13.20"
  },
  "devDependencies": {
    "@stoplight/prism-cli": "^4.10.0"
  },
  "version": "0.0.1"
}
